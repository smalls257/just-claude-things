id: rate-limiting
display: Rate limiting middleware
layer: http-inbound
priority: 60
files: ["src/**/*.cs"]
signal:
  cs: ["AddRateLimiter\\s*\\(", "UseRateLimiter\\s*\\("]
extract: enclosing-method-or-extension

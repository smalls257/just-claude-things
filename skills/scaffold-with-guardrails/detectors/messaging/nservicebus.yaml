id: nservicebus
display: NServiceBus endpoint configuration
layer: messaging
priority: 80
files: ["src/**/*.cs"]
signal:
  cs: ["EndpointConfiguration\\s*\\(", "UseNServiceBus\\s*\\("]
extract: enclosing-method-or-extension

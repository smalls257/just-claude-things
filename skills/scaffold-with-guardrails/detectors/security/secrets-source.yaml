id: secrets-source
display: Secrets source (KeyVault / EnvVars / UserSecrets)
layer: security
priority: 70
files: ["src/**/*.cs"]
signal:
  cs: ["AddAzureKeyVault\\s*\\(", "AddUserSecrets\\s*\\(", "AddEnvironmentVariables\\s*\\("]
extract: enclosing-method-or-extension

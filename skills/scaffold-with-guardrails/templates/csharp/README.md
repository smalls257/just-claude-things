# C# Template

See `scaffold.md` for the full scaffold instructions the skill follows.

Requirements: .NET 9 SDK, semgrep CLI. `CSharpFunctionalExtensions` and
`NetArchTest.Rules` are added via NuGet by the scaffold commands.

# Task: emit migration SQL for {{MODULE}}

## File to emit

`{{MIGRATIONS_DIR}}/0001_initial_schema.sql` (if not present)

If a migration file already exists, APPEND a per-module block to the end of the most recent `*_initial_schema.sql`.
Default `{{MIGRATIONS_DIR}}` is `migrations/`; use `db/migrations/` if that directory already exists.

## Inputs (from JSON task)

```json
{{TASK_JSON}}
```

## Canonical header (verbatim — only on file create, not append)

```sql
-- Generated by scaffold Phase-2 from docs/tech-design/{{SLUG}}.md
-- Regenerated on every Phase-2 run. Throwaway post-dev-deploy.
-- Source modules: {{MODULE_LIST}}
```

## Body template (per module)

```sql

-- ============================================================================
-- Module: {{MODULE}}
-- ============================================================================
{{for each table in inputs.tables:}}

-- Source: <module name="{{MODULE}}"><entities>{{table.entity}}</entities></module>
{{table.sql}};
{{end for}}
```

## Verification

- file exists at the migrations path
- file begins with the canonical header on the first emission for the module list
- each table's SQL appears verbatim from the tech-design fence

# Task: emit unit-test stub for {{MODULE}}.{{ENTITY}}

## File to emit

`tests/{{APP_NAME}}.Tests.Unit/{{MODULE}}/{{ENTITY}}Tests.cs`

## Inputs (from JSON task)

```json
{{TASK_JSON}}
```

## Canonical header (verbatim)

```csharp
// Generated by scaffold Phase-2 from docs/tech-design/{{SLUG}}.md
// Source: <module name="{{MODULE}}"><entities>{{ENTITY}}</entities></module>
// Devs: edit freely. Re-running Phase-2 overwrites. Commit before re-run.
```

## Body template

```csharp
namespace {{APP_NAME}}.Tests.Unit.{{MODULE}};

using Xunit;
using {{APP_NAME}}.Domain.{{MODULE}};

public class {{ENTITY}}Tests
{
    [Fact]
    public void Placeholder()
    {
        // TODO: assert invariants once {{ENTITY}}.Create is implemented.
        Assert.True(true);
    }
}
```

## Verification

- file exists at the path above
- contains the `Placeholder` test
- file contains no unsubstituted `{{...}}` tokens
- `dotnet build` from repo root exits 0

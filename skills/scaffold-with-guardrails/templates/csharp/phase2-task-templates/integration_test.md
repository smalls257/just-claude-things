# Task: emit integration-test stub for {{MODULE}}.{{METHOD}} {{PATH}}

## File to emit

`tests/{{APP_NAME}}.Tests.Integration/{{MODULE}}/{{METHOD}}_{{PATH_SAFE}}_Tests.cs`

`{{PATH_SAFE}}` = path with `/` replaced by `_`, leading `_` stripped, `{` and `}` removed.
Example: `/authors/{id}` → `authors_byId`.

## Inputs (from JSON task)

```json
{{TASK_JSON}}
```

## Canonical header (verbatim)

```csharp
// Generated by scaffold Phase-2 from docs/tech-design/{{SLUG}}.md
// Source: <module name="{{MODULE}}"><routes>{{METHOD}} {{PATH}}</routes></module>
// Devs: edit freely. Re-running Phase-2 overwrites. Commit before re-run.
```

## Body template

```csharp
namespace {{APP_NAME}}.Tests.Integration.{{MODULE}};

using Xunit;

public class {{METHOD}}_{{PATH_SAFE}}_Tests
{
    [Fact]
    public void Placeholder()
    {
        // TODO: WebApplicationFactory + HttpClient test once endpoint is implemented.
        Assert.True(true);
    }
}
```

## Verification

- file exists at the path above (class name and file name match)
- `dotnet build` from repo root exits 0

#!/usr/bin/env bash
set -uo pipefail
HOOK_NAME="pre-commit"
HOOK_DIR="$(dirname "$0")/pre-commit.d"
[ "${1:-}" = "--self-test" ] && { echo "pre-commit dispatcher reachable"; exit 0; }
# shellcheck source=./_dispatcher
. "$(dirname "$0")/_dispatcher"
